apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dungeonomics
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dungeonomics
                port:
                  number: 8000